[Unit]
Description=Socat port forwarding from port %i to {{ socat_target_ip }}:%i
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/socat TCP6-LISTEN:%i,bind=[::],fork,reuseaddr TCP:{{ socat_target_ip }}:%i
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
